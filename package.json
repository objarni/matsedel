{
  "name": "puregerm",
  "version": "1.0.0",
  "scripts": {
    "start": "spago bundle-app --watch",
    "format": "purs-tidy format-in-place **/*.purs"
  },
  "dependencies": {
    "esbuild": "0.19.2"
  }
}
